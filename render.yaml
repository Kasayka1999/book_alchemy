services:
  - type: web
    name: book-alchemy
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python download_photos.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.15
